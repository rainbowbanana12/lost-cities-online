# Lost Cities Online v5.4

기존 Hybrid v5.3 Mobile UI와 게임 규칙을 그대로 유지하면서 인터넷 서버 배포가 가능하도록 연결 구조를 바꾼 버전입니다.

## 온라인에서 달라진 점
- 서로 다른 Wi-Fi, LTE/5G, PC/휴대폰 사이에서도 동일한 공개 주소로 접속 가능
- 방 코드는 기존과 동일한 4자리 방식
- 방을 만든 뒤 주소에 `?room=ABCD`가 붙어 상대에게 URL째 공유 가능
- 새로고침 또는 잠깐의 네트워크 끊김 뒤 자동 재접속
- 브라우저의 localStorage에 방 코드와 개인 재접속 토큰 저장
- 연결이 끊겨도 5분 동안 방을 유지
- 6시간 동안 활동이 없는 방은 자동 삭제
- `/healthz` 상태 확인 엔드포인트 추가
- Dockerfile / Procfile 포함

## 로컬 테스트
Node.js 20 이상:

```bash
npm install
npm start
```

브라우저에서 `http://localhost:3000`

## 인터넷 배포
Node.js 웹 서버 또는 Docker 컨테이너를 실행할 수 있는 서비스에 이 폴더 전체를 배포합니다.

필수 조건:
- 실행 명령: `npm start`
- 서비스가 지정하는 `PORT` 환경변수를 그대로 사용
- WebSocket/Socket.IO 연결 허용
- HTTPS 사용 권장
- 인스턴스는 1개로 운영

배포가 끝나면 두 플레이어 모두 같은 공개 HTTPS 주소로 접속하면 됩니다.

예:
`https://your-game.example`

한 명이 방을 만들면 URL이:
`https://your-game.example/?room=ABCD`
형태가 되므로 이 주소를 상대에게 보내면 방 코드가 자동 입력됩니다.

## 현재 프로토타입의 서버 저장 방식
방과 게임 상태는 서버 메모리에 저장됩니다. 따라서 서버 프로세스가 재시작되거나 배포가 다시 이루어지면 진행 중인 방은 사라집니다.

여러 서버 인스턴스를 동시에 띄우거나 재배포 후에도 방을 보존하려면 Redis/DB 기반 저장소와 Socket.IO 어댑터를 추가해야 합니다. 현재 버전은 친구끼리 온라인으로 플레이하는 단일 서버 프로토타입에 맞춰져 있습니다.


## v5.5 Landscape Mobile
- 휴대폰 가로모드 전용 한 화면 레이아웃
- 상대 탐험 / 버림패 / 내 탐험 / 점수 / 손패를 한 화면에 배치
- 5개 색상을 가로 스크롤 없이 동시에 표시
- 손패 8장을 가능한 한 한 화면에 표시
- 모바일 탭 조작 유지
- 가로모드에서는 오른쪽 드로우/재시작 버튼 사용
- 목표카드는 상단 접이식 버튼 유지
- PC/세로 모바일은 기존 v5.4 레이아웃 유지
- safe-area / 노치 대응


## v5.6 Mobile bug fix
- 서버가 각 브라우저에 `myTurn`, `canPlayCard`, `canDrawCard`를 직접 전달
- 모바일 턴 판정을 클라이언트 추측 대신 서버 판정으로 통일
- 덱 잔여 장수 필드 정규화, undefined 표시 방지
- 가로모드의 고정 최소 높이 제거
- 손패 영역을 가장 크게 확보하고 가로 스크롤 fallback 추가
- 화면 높이가 극단적으로 낮으면 세로 스크롤 허용


## v5.7 Portrait Mobile UX
- 휴대폰 세로모드 5색 탐험/버림패를 한 화면 폭에 모두 표시
- 손패 8장도 한 화면 폭에 모두 표시
- 상대 탐험 영역 축소
- 모바일 카드 플레이: 카드 선택 → `선택 카드 버리기` 또는 `내 탐험에 놓기`
- 모바일 버림패 드로우: 버림패 선택 → `버림패에서 가져오기`
- 모바일에서 탐험칸/버림패 탭만으로 즉시 행동하지 않도록 변경
- PC/폰 공통: 별도 색깔별 점수 바 제거
- 내 탐험 각 색 제목에 실시간 점수 표시
- 내 탐험 제목 옆에 실시간 합계 표시
- 가로모드 전용 강제 레이아웃 폐기


## v5.8 Layout readability
- 내 탐험 점수 표기: `5장 | 점수: 35`
- 양수 파랑, 음수 빨강, 0점 흰색
- 점수 크기/굵기 강화
- 세로폰 내 탐험 카드를 세로 방향으로 쌓아 숫자 노출
- 색깔 탐험칸 밖으로 카드가 넘어가지 않도록 제한
- 상대 탐험 영역 추가 축소
- 세로폰 손패 영역을 카드 높이에 맞춰 축소
- PC 손패 컨테이너 자동 높이화


## v5.9 Portrait Row Board
- 세로폰 탐험 레이아웃을 5열 방식에서 5개 가로 행 방식으로 변경
- 상대 탐험/내 탐험 모두 색상별 한 줄 사용
- 각 탐험 행 안에서 카드가 좌→우로 놓이며 다른 색 행을 침범하지 않음
- 카드가 많아질 때만 약하게 겹치도록 조정
- 버림패는 5색 한 줄 유지
- 손패 8장은 한 화면 폭 유지
- PC 레이아웃은 변경하지 않음

## v6.0 Flat GitHub Upload
- `public/` 폴더를 제거했습니다.
- GitHub 웹 업로드가 폴더를 풀어버려도 정상 작동하도록 변경했습니다.
- `index.html`, `game.js`, `style.css`는 저장소 루트에 둡니다.
- 앞으로 ZIP을 풀고 보이는 파일을 전부 GitHub 저장소 첫 화면에 업로드하면 됩니다.


## v6.1 Unified Expedition Rows
- PC와 모바일 모두 탐험 5색을 세로 5행 구조로 통일
- 각 색 행 안에서 카드가 좌→우로 배치
- 상대 탐험/내 탐험 동일 구조
- 내 탐험 행 왼쪽에 장수 및 색상별 점수 표시
- 카드가 많아질 때만 약하게 겹쳐 다른 색 영역 침범 방지
- 버림패 5색 한 줄과 손패 구조는 유지


## v6.2 Compact Board Rebalance
- 전체 외곽 및 섹션 내부 여백 축소
- PC: 상대 탐험(왼쪽) / 내 탐험(오른쪽) 2열 구조
- PC: 버림패는 양쪽 탐험 아래 전체폭, 손패 왼쪽 / 뽑기 더미 오른쪽
- PC: 목표카드는 상단 별 아이콘 hover 팝오버
- 모바일: 탐험 제목 열 너비 축소, 카드와 제목 간격 축소
- 모바일: 탐험/버림패/손패 세로 공백 최소화
- PC와 모바일 카드 크기 별도 설정
- 기존 점수 색상 및 `5장 | 점수: 35` 표기 유지


## v6.3 Focus Board
- 웹의 상대/내 탐험 좌우 2열 배치를 폐기하고 세로 흐름으로 복원
- 웹도 모바일과 같은 카드 선택 + 버리기/내 탐험 놓기 + 뽑기 방식 사용
- 모바일 상대 탐험 높이를 더 압축하고 내 탐험을 강조
- 손패 남은 카드 수 표기 제거
- 목표카드 아이콘을 우측 상단으로 이동
- 탐험 제목/장수/점수와 카드 사이 여백 축소
- 웹/모바일 모두 카드 크기 확대


## v6.4 Desktop Side Panel
- 웹: 왼쪽 탐험 영역 / 오른쪽 버림패+목표카드 패널
- 웹: 하단 한 줄에 손패 / 현재 단계의 행동 버튼 2개
- 플레이 단계: 선택 카드 버리기 / 내 탐험에 놓기
- 뽑기 단계: 버림패에서 가져오기 / 덱에서 뽑기
- 웹과 모바일 모두 손패는 드래그가 아니라 클릭/탭 선택 방식
- 모바일은 기존 v6.3 구조 유지, 버림패 영역 세로 높이만 추가 축소


## v6.5 Side-Label Compact Board
- 웹/모바일 공통: 상대 탐험, 내 탐험, 버리는 카드, 내가 가진 카드 제목을 상단이 아닌 왼쪽 정보열로 이동
- 상대 탐험 왼쪽: 상대 닉네임 표시
- 내 탐험 왼쪽: 내 아이디 + 탐험 합계 표시
- 탐험 행 정보와 카드 사이 여백 추가 축소
- 모바일 순서: 상대 탐험 → 내 탐험 → 버리는 카드 → 내가 가진 카드 → 행동 버튼
- 모바일 목표카드는 기존 우측 상단 팝업 유지
- 웹 목표카드는 오른쪽 상단 상시 표시, 버림패를 바로 아래 배치
- 웹 하단: 왼쪽 정보열 + 손패 8장 / 오른쪽 현재 단계 버튼 2개
- 손패 남은 카드 수 표시 제거


## v6.6 Mobile Top Labels
- 웹 v6.5 레이아웃은 유지
- 모바일만 상대 탐험 / 내 탐험 / 버리는 카드 / 내가 가진 카드를 다시 각 영역 상단으로 복원
- 제목줄 높이를 14~16px로 제한해 세로 길이 증가 최소화
- 상대 닉네임, 내 아이디, 탐험 합계를 제목줄 한 줄 안에 함께 표시
- 상대 탐험은 계속 압축, 내 탐험 가독성은 유지
- 목표카드는 기존 우측 상단 팝업 유지


## v6.7 Color Flow Board
- 웹/모바일 모두 `상대 탐험 → 버림패 → 내 탐험`을 색깔별 한 행에 정렬
- 빨강/노랑/초록/파랑/흰색의 카드 흐름을 한눈에 추적 가능
- 웹: 손패 드래그 복원
- 웹: 내 탐험 아무 영역에 드롭하면 카드 색에 맞는 탐험행에서 처리
- 웹: 버림패 전체 영역이 하나의 드롭 타깃이며 카드 색에 따라 자동 분류
- 웹: 버림패 맨 위 카드 클릭으로 가져오기, 덱 버튼으로 덱 드로우
- 모바일: 기존 탭 선택 + 행동 버튼 유지
- 모바일: 상대 27% / 버림 14% / 내 탐험 59% 비중으로 내 탐험 가독성 우선
- 양쪽 모두 viewport 높이에 맞춰 스크롤 없는 한 화면 플레이를 목표로 재설계
- 목표카드는 우측 상단 별 아이콘 팝업


## v6.8 Stable Color Flow Rows
- v6.7에서 색상 행이 접혀 손패만 보이던 레이아웃 버그 수정
- 불안정한 `display: contents` 매트릭스 제거
- 빨강/노랑/초록/파랑/흰색 각각 실제 DOM 행으로 생성
- `상대 → 버림패 → 나` 한 줄 흐름 유지
- 웹 드래그 및 버림패 자동 색상 분류 유지
- 모바일 선택 버튼 방식 유지


## v6.9 Desktop Wireframe Layout
- 사용자 제공 웹 도면의 전체 배치/비율을 기준으로 데스크톱 UI 재구성
- 좌측 목표카드 고정 5칸
- 중앙 `상대 탐험 | 버림패 | 내 탐험` 3열과 5개 색상 행
- 하단 큰 손패 8장, 우측 하단 덱
- 상대/내 탐험 카드와 숫자 크기 확대
- 카드가 작은 clamp 값으로 쪼그라드는 현상 방지
- 웹은 드래그 조작 유지
- 모바일 레이아웃/CSS는 이번 버전에서 변경하지 않음


## v7.0 Direct Flow Rows
- v6.9에서 중앙 5색 보드가 비어 보이던 문제 수정
- 숨겨진 DOM을 cloneNode로 복제하던 구조 완전 제거
- 게임 state에서 상대 탐험 / 버림패 / 내 탐험 5색 행을 직접 렌더링
- v6.9 데스크톱 도면 배치와 카드 크기/비율 유지
- 웹 드래그 유지
- 버림패 어느 칸에 드롭해도 카드 색에 따라 자동 분류
- 내 탐험 영역에 드롭하면 카드 색에 맞는 탐험으로 자동 처리
- 모바일 조작 방식은 기존 선택 버튼 방식 유지
